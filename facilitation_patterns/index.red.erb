% patterns_by_category.each do |category, patterns|
h3. <%= category.titleize %>

% patterns.each do |pattern|
* "<%= pattern.name %>":<%= pattern.symbol %>.html
% end  

% end

----

what about games?  modeling?
